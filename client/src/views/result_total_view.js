const PubSub = require('../helpers/pub_sub.js');
const RequestHelper = require('../helpers/request_helper.js');

const RequestTotalView = function(element) {
  this.element = element;
};

RequestTotalView.prototype.bindEvents = function () {

};
