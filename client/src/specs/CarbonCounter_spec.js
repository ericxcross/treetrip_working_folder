const CarbonCounter = require('../models/CarbonCounter.js');
const assert = require('assert');

describe('CarbonCounter',()=>{
    let carbonCounter = new CarbonCounter(http://localhost:3000/api/transportmodes)


});